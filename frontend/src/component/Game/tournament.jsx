import React from 'react'

const Tournament = () => {
  const apiUrl = `${window.location.hostname}`
  console.log(apiUrl)
  return (
    <div>
      Tournament
    </div>
  )
}

export default Tournament
